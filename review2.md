# Laravel Lesson レビュー ②

## Todo 編集機能

### @method('PUT')を記述した行に何が出力されているか

input タグ

### find メソッドの引数に指定している ID は何の ID か

DB に保存されているレコードの ID

### find メソッドで実行している SQL は何か

SELECT \* from todos WHERE id = 選択した id limit 1

### find メソッドで取得できる値は何か

選択した id の todo インスタンス

### save メソッドは何を基準に INSERT と UPDATE を切り替えているのか

属性の変更と保存

## Todo 論理削除

### trait と class の違いとは

クラスはインスタンス化できるがトレイトはインスタンス化できない
トレイトは 1 つのクラスに複数のトレイトを追加することができる

### trait を使用するメリットとは

クラス間で共通の機能を共有することができる

## その他

### TodoController クラスのコンストラクタはどのタイミングで実行されるか

ルート定義されたあと

### Request クラスから FormRequest クラスに変更した理由

Request クラスを継承してフォーム入力のバリデーション処理をより簡単に管理するため

### $errors の has メソッドの引数・返り値は何か

引き数は入力欄の name 属性、返り値は真偽値を返します。

### $errors の first メソッドの引数・返り値は何か

引き数は入力欄の name 属性、返り値は文字列を返します。

### フレームワークとは何か

アプリケーション開発においてよく利用される機能をパーツとして備えた枠組み

### MVC はどういったアーキテクチャか

開発効率を高めるために作られたアーキテクチャ

### ORM とは何か、また Laravel が使用している ORM は何か

ORM は直接 SQl クエリの作成をすることなくデータベースを操作できる機能

Larabel が使用している ORM は Eloquent ORM

### composer.json, composer.lock とは何か

composer.json はライブラリやパッケージを定義するための設定ファイル

composer.lock はプロジェクトで実際にインストールされたパッケージの具体的なバージョンを記録するファイル

### composer でインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか

vendor ディレクトリ
